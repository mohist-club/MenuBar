# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode (万一以后有人拿 Xcode 打开)
*.xcodeproj
*.xcworkspace
DerivedData/
xcuserdata/
*.xcuserstate

# 打包产物,不进版本库(通过 Release 分发,不放进仓库)
*.app
*.dmg
*.icns

# macOS
.DS_Store

# 密钥、签名相关(千万别提交进仓库)
*.p12
*.mobileprovision
*_dsa_priv.pem
sparkle_private_key*
